Bootstrap: docker
From: ubuntu:18.04

%environment
  TZ=UTC
  DEBIAN_FRONTEND=noninteractive
 
%post
    export TZ=UTC
    export DEBIAN_FRONTEND=noninteractive

# Install the commonly dependencies 
    apt-get update && apt-get install -y  \
        apt-utils \
        pkg-config \
        autoconf \
        g++ \
        gcc \
        git \
        libhts-dev \
	python3-dev \
	python3-setuptools \
	libtiff5-dev \
	libjpeg8-dev \
	libopenjp2-7-dev \
	zlib1g-dev \
    	libfreetype6-dev \
	liblcms2-dev \
	libwebp-dev \
	tcl8.6-dev \
	tk8.6-dev python3-tk \
    	libharfbuzz-dev \
	libfribidi-dev \
	libxcb1-dev \
        python3-pip \

# Install pandas
cd opt
mkdir pandas
pip3 install Pillow
python3 -m pip install --upgrade pip
python3 -m pip install --upgrade Pillow
pip3 install pandas
pip3 install argparse
pip3 install matplotlib

%runscript
python3 "$@"